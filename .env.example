# Development Settings
# Set to 'false' to use production Firebase instead of emulators in dev mode
# VITE_USE_EMULATORS=false

# Firebase Configuration (optional - project has defaults)
# Only override if using a different Firebase project
# VITE_FIREBASE_API_KEY=your-api-key
# VITE_FIREBASE_PROJECT_ID=your-project-id
